basic:
  title: "Basic Test Bench"
  vars:
    input:
      - a: int
      - b: int
    output:
      - result
  cases:
    - input:
        - 2
        - 3
      output:
        - 5
    - input:
        - 6
        - 4
      output:
        - 10
  feedback:
    expected:
      - regex: '\+'
        feedback: Deberías usar el operador suma
      - regex: for
        feedback: Deberías usar el bucle for
